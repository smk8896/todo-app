<div class="well">

  <%= link_to user_task.description, user_task, remote: true, method: :delete, data: {
    confirm: 'Are you sure?'} %>

  <%= link_to fa_icon('pencil'), edit_user_task_path(user_task), remote: true %>

    <span class = "pull-right">
  <%= user_task.due.stamp("Apr 15, 1987") if user_task.due %>
  </span>
</div>